const menuEl = document.querySelector('.js-menu');
const switchThemeEl = document.getElementById('theme-switch-toggle');
const bodyEl = document.querySelector('body')

export default {
    menuEl,
    switchThemeEl,
    bodyEl,
}